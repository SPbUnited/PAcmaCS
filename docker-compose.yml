services:
  serviz:
    build: ./serviz
    ports:
      - "8000:8000"
    volumes:
      - ./serviz:/app
      - ipc:/tmp
    init: true

  larcmacs:
    build: ./LARCmaCS
    volumes:
      - ./LARCmaCS:/app
      - ipc:/tmp
      # - /tmp:/tmp
    init: true

volumes:
  ipc:
